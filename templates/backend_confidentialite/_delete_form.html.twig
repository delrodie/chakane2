<form method="post" action="{{ path('app_backend_confidentialite_delete', {'id': confidentialite.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette politique de confidentialité?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ confidentialite.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
