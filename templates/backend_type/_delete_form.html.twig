<form method="post" action="{{ path('app_backend_type_delete', {'id': type.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ce type?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
    <button class="btn btn-danger">Suppression</button>
</form>
