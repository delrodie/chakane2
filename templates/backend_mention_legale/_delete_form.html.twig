<form method="post" action="{{ path('app_backend_mention_legale_delete', {'id': mention_legale.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette mention légale?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mention_legale.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
