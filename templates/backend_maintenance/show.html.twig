{% extends 'base.html.twig' %}

{% block title %}Maintenance{% endblock %}

{% block body %}
    <h1>Maintenance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maintenance.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ maintenance.statut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_maintenance_index') }}">back to list</a>

    <a href="{{ path('app_backend_maintenance_edit', {'id': maintenance.id}) }}">edit</a>

    {{ include('backend_maintenance/_delete_form.html.twig') }}
{% endblock %}
