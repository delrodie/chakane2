{% extends 'backend_layout.html.twig' %}

{% block title %}{{ parent() }} Gestion liste des produits{% endblock %}

{% block body %}

    <div class="container-fluid">
        <div class="card bg-light-info shadow-none position-relative overflow-hidden">
            <div class="card-body px-4 py-3">
                <div class="row align-items-center">
                    <div class="col-9">
                        <h4 class="fw-semibold mb-8">Gestion de la maintenance</h4>
                        <nav aria-label="breadcrumb">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a class="text-muted" href="#">Paramètres</a></li>
                                <li class="breadcrumb-item"><a class="text-muted" href="#">Maintenance</a></li>
                                <li class="breadcrumb-item" aria-current="page">Enregistrement</li>
                            </ol>
                        </nav>
                    </div>
                    <div class="col-3">
                        <div class="text-center mb-n5">
                            <span class="head-icon"><i class="ti-file"></i></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <section>
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <div class="row">
                                <div class="col"><h5 class="mb-2 fw-semibold fs-4">Détails</h5></div>

                            </div>

                        </div>
                        <div class="card-body border-top p-4">
                            <h2 class="fs-6 fw-semibold mb-3">{{ cgu.titre }}</h2>
                            <div class="fs-4" style="text-align:justify;">
                                {{ cgu.contenu|raw }}
                            </div>
                            <div class="row mt-5">
                                <div class="col text-end"><a href="{{ absolute_url(path('app_backend_cgu_edit',{'id': cgu.id})) }}" class="btn btn-primary"> Modifier</a></div>
                                <div class="col text-left">
                                    {{ include('backend_cgu/_delete_form.html.twig') }}
                                </div>


                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>


    </div>

{% endblock %}