<form method="post" action="{{ path('app_backend_politique_retour_delete', {'id': politique_retour.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette politique?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ politique_retour.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
