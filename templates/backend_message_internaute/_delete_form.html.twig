<form method="post" action="{{ path('app_backend_message_internaute_delete', {'id': message_internaute.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message_internaute.id) }}">
    <button class="btn">Delete</button>
</form>
