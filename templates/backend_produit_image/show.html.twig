{% extends 'base.html.twig' %}

{% block title %}ProduitImage{% endblock %}

{% block body %}
    <h1>ProduitImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit_image.id }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ produit_image.media }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_produit_image_index') }}">back to list</a>

    <a href="{{ path('app_backend_produit_image_edit', {'id': produit_image.id}) }}">edit</a>

    {{ include('backend_produit_image/_delete_form.html.twig') }}
{% endblock %}
